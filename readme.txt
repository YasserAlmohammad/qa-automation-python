# QA Automation (Python + pytest)

Learning project to practice Python test automation with **pytest**.

## Setup
```bash
python -m venv .venv
# Windows (PowerShell)
.\.venv\Scripts\Activate.ps1
pip install -r requirements.txt
